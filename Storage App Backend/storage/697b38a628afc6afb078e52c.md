**prompts**



Remove all existing lifeline logic from the application and re-implement the lifeline feature from scratch according to the requirements below, as the current implementation has bugs and no proper state persistence.



The game has 3 lifelines:



50-50 – removes 2 incorrect options out of 4.



Extra Time – adds 30 seconds to the current question timer.



Question Swap – replaces the current question with a new one based on the team’s opinion and permanently removes the revealed question from the question pool.



There are two lifeline UI components:



A display-only lifeline component that shows lifeline availability for the team.



A QuestionOverlay lifeline component where users can select lifelines.



Users can select lifelines only inside the QuestionOverlay. On clicking a lifeline, show a confirmation dialog; only if confirmed should the lifeline be applied.



Each team can use only 2 lifelines total during the entire game, and each lifeline can be used only once. Once used, the lifeline must be disabled and marked red in both lifeline components for the rest of the game.



Implement lifeline state persistence in the Teams model using an array of objects with the following default structure:

lifelines: \[{ "50-50": false }, { "QUESTION-SWAP": false }, { "EXTRA-TIME": false }]

When a user confirms a lifeline, update the corresponding lifeline value to true. If a lifeline value is true, render it in red, disable reselection, and prevent backend reuse.



The database must be the single source of truth. Lifeline state must persist across refreshes and navigation, and both frontend and backend must strictly prevent reuse, over-usage (more than 2 lifelines), or component desynchronization.







---

GOAL:

Update the Question feature so the UI renders correctly based on question type and time limit.



BACKEND TASKS (MODIFY ONLY):

1\. Update the existing Question MongoDB schema to include:

   - type ("text" | "image")

   - questionText (string)

   - questionImage (string | null) → stores relative path like "/uploads/questions/img1.png"

   - category (string)

   - timeLimit (number, seconds)

   - options (array)

2\. Ensure express.static is configured to serve local images from /uploads.

3\. Ensure the existing question-fetch API returns:

   type, questionText, questionImage, category, timeLimit, options

4\. Do NOT add seeders or auto-generated data.



FRONTEND TASKS (MODIFY ONLY):

1\. Update the existing QuestionOverlay (or equivalent) component to:

   - Display question category at the top

   - Always display question text

   - Conditionally render an image ONLY if question.type === "image"

   - Load image using backend base URL (env-based)

2\. Implement a per-question countdown timer using question.timeLimit

3\. Reset the timer whenever the question changes

4\. Automatically trigger onTimeUp() when the timer reaches zero

5\. Keep styling consistent with the existing UI



IMPORTANT CONSTRAINTS:

\- Question data comes from MongoDB manually, do not assume seeded data

\- Do NOT use absolute filesystem paths

\- Do NOT move images to cloud storage

\- Do NOT change unrelated schemas, routes, or authentication

\- Follow the existing folder structure



OUTPUT REQUIRED:

\- Updated Question schema code

\- Any necessary backend configuration changes

\- Updated QuestionOverlay component code

\- Brief explanation of changes





----------------------------------------------------------

Q100.find the cartoon character names

Q101.find the cartoon character names

Q102.find the cartoon character names

Q103.spot the difference from the give picutures

Q104.spot the difference from the give picutures

Q105.Find The Give Words From the picture Words : 1.Timerber truck 2.pipeline 3.traffic

Q106.Find The Give Words From the picture Words : 1.signboard 2.bulldozwe 3.excavator

Q107. Answer The Questions In the Image

Q108. Answer The Questions In the Image

Q109. Answer The Questions In the Image ?

Q110. Answer The Questions In the Image ?



100,101,102 cartoons 
/uploads/questions/Q100.jpeg ADD THIS PATH TO THE IMAGE FOR 100 QUESTION TO 100 AND SO ON LIKE THIS




